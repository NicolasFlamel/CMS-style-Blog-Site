<h1>Blog</h1>
<ol>
    {{#each blogData}}
    <li>
        <h2>{{this.title}}</h2>
        <p>{{this.body}}</p>
        <p class="text-end"><b>Posted by {{this.user.username}} on {{formatDate this.dateCreated}}</b></p>
    </li>
    {{/each}}
</ol>